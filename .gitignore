# Dados sensíveis — nunca versionar
static/config.local.js
*.csv
*.xlsx
*.xls
.env
# Briefings e materiais de cliente — nunca versionar (repo público)
07-outros/
Briefing*.md
Briefing

# Conteúdo pessoal do usuário — fica só na máquina local (repo público)
memory/*
!memory/.gitkeep
sessoes/
perfil-do-aluno.md
*.key
credentials*.json

# Claude Code — configurações locais (settings.json e skills/ são do projeto e devem ser versionados)
.claude/*
!.claude/settings.json
!.claude/skills/

# Arquivos gerados — gerenciados exclusivamente pelo CI (reindex-weekly.yml)
# Nunca commitar localmente para evitar conflitos com o GitHub Actions
docs/

# Python
__pycache__/
*.pyc

# Sistema
.DS_Store
node_modules/
output/
.venv/
__pycache__/
*.pyc
