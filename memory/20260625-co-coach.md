# Handoff — co-coach — 2026-06-25 (sessão 2)

## O que foi feito nesta sessão

- **Push de 3 arquivos locais pendentes:**
  - `.claude/settings.json` — permissões de Bash adicionadas
  - `kb/vscode-interface.md` — artigo sobre interface do VS Code indexado na KB
  - `kb/vscode-keybindings.md` — artigo sobre atalhos do VS Code indexado na KB
- **Distribuição de skills** — disparados 6 workflows `install-skills-remote.yml` com `overwrite: true` para instalar as 30 skills co-coach em todos os outros repos:
  - `victorauad/iracing_analysis`, `victorauad/alamtoco`, `victorauad/SMPL`
  - `victorauad/app-creator-pro`, `victorauad/Antigravity_Worc`, `victorauad/auaderacodes_iracing`

## Estado atual (herdado da sessão anterior + esta sessão)

- Branch `main` sincronizado com GitHub (último commit `54d6ce8`)
- 30 skills em `skills/*/SKILL.md` distribuídas para todos os repos via workflow
- Os 6 workflows estavam `in_progress` ao encerrar — resultado final não confirmado
- Sistema SDD completo: `requirements.md`, `design.md`, `TASKS.md` na raiz

## Próximos passos

1. Verificar se os 6 workflows de distribuição de skills concluíram com sucesso em [github.com/victorauad/co-coach/actions](https://github.com/victorauad/co-coach/actions)
2. Abrir sessão em um repo alvo (ex: `iracing_analysis`) e confirmar que `.claude/skills/` tem as 30 skills
3. **iOS Shortcut** — testar fluxo end-to-end: URL no iPhone → Issue com label `add-link` → card no feed em <3 min
4. `TASKS.md` tem diff local não commitado — revisar e commitar se relevante
5. Criar `docs/proficiency.json` para fechar o loop da skill `co-coach-quiz`

## Decisões e contexto importantes

- Workflow `install-skills-remote.yml` é `workflow_dispatch` manual, um repo por vez; foi disparado 6x via `gh workflow run`
- `overwrite: true` garante atualização mesmo em repos com versões antigas
- **`static/` é a regra** — HTMLs que sobrevivem ao rebuild vão em `static/`, nunca em `docs/` diretamente
- SDD como metodologia — consultar `requirements.md`, `design.md`, `TASKS.md` antes de qualquer tarefa não trivial

## Arquivos relevantes

- `.claude/settings.json` — permissões de Bash do projeto
- `kb/vscode-interface.md`, `kb/vscode-keybindings.md` — novos artigos na KB
- `.github/workflows/install-skills-remote.yml` — workflow de distribuição de skills
- `TASKS.md` — backlog priorizado (verificar diff local)
- `skills/` — 30 skills co-coach canônicas
- `requirements.md`, `design.md` — spec SDD do sistema
- **Pesquisa e indexação de SDD** — 11 artigos sobre Spec-Driven Development adicionados à KB (Thoughtworks, Martin Fowler, Addy Osmani, Red Hat, DeepLearning.AI, JetBrains, arxiv, DataCamp, Kiro/AWS, alexop.dev, BCMS); KB passou de 108 para 119 cards
- **Adoção de SDD no projeto** — criados `requirements.md` (histórias de usuário + critérios de aceitação) e `design.md` (arquitetura, decisões técnicas, restrições) na raiz do repo
- **CLAUDE.md atualizado** — adicionado ciclo SDD (Specify→Plan→Implement→Validate), referências aos 3 documentos de spec, estrutura de repositório atualizada (32 skills, pasta `static/`, `design.md`/`requirements.md`)
- **Gerenciador local** — `static/gerenciador.html` (interface com 4 seções: skills, KB, sync, config) + `scripts/server.py` (servidor Python stdlib na porta 8765)
- **Diagrama explicativo** — `static/aprenda.html` (explicação visual do sistema para não-devs: 3 camadas, passo a passo, glossário, diagrama Mermaid)
- **Auditoria co-coach-review** — validação spec vs. realidade identificou 5 divergências
- **Divergências corrigidas:**
  - `aprenda.html` e `gerenciador.html` movidos de `docs/` para `static/` (sobrevivem ao rebuild do CI)
  - `build-site.py` atualizado: copia `static/` para `docs/` a cada rebuild + rodapé com stats por tema + data do rebuild
  - `design.md` atualizado: seção `static/` documentada com regra de uso
- **Todos os commits pusheados** para `main` — CI ativo

