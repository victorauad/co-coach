# Handoff — co-coach — 2026-07-06

## O que foi feito nesta sessão
- Rodado `/co-coach-wizard` para criar um projeto novo do zero: **AuaDriving**
- Explicado o mecanismo de sync de skills (`.github/workflows/sync-skills.yml` + `config/sync-targets.yml`): é push-based, disparado quando `skills/` muda no co-coach, e só alcança repositórios cadastrados na lista
- Gerado o setup completo do AuaDriving em `~/Projetos/AuaDriving`: `CLAUDE.md`, estrutura de pastas (`memory/`, `src/`, `scripts/`, `docs/`, `data/raw/`, `data/output/`), `.claude/settings.json` e 5 skills copiadas (`co-coach-setup`, `co-coach-review`, `co-coach-notebooklm`, `co-coach-handoff`, `co-coach-ui`)
- Criado `memory/backlog-inicial.md` no AuaDriving com as 6 primeiras tarefas
- Cadastrado `victorauad/AuaDriving` em `config/sync-targets.yml` do co-coach
- Criado o repositório GitHub `victorauad/AuaDriving` (privado) via `gh repo create --source=. --push`
- Commitado e enviado o cadastro do sync-targets.yml no co-coach (precisou de `git pull --rebase` antes, pois o remoto tinha um commit de rebuild automático do site)

## Estado atual
- AuaDriving existe como repo GitHub com setup inicial completo, mas **sem código ainda** — nenhum parser de `.ibt`, nenhum webapp, nenhuma integração com Claude API
- Não foram criados `requirements.md`/`design.md`/`TASKS.md` (Spec-Driven Development) no AuaDriving — decisão explícita do usuário, que ainda vai trazer a KB do NotebookLM e o design do Figma antes de formalizar a spec
- co-coach está sincronizado com origin/main (commit `b8998f4`)

## Próximos passos
1. No AuaDriving: trazer um par de arquivos `.ibt` de exemplo (telemetria do usuário + referência) e testar a leitura em Python (biblioteca sugerida: `pyirsdk`/`irsdk`)
2. No AuaDriving: trazer o conteúdo da KB do NotebookLM e o design do Figma, depois rodar `/co-coach-sdd` para formalizar `requirements.md` e `design.md`
3. Validar que o próximo push em `skills/` do co-coach realmente propaga as skills para o AuaDriving (teste do sync automático recém-cadastrado)
4. Retomar o backlog do gap analysis do co-coach (ver sessões anteriores: popular KB com conteúdo de service-as-software, ainda 0 arquivos no tema)

## Decisões e contexto importantes
- **AuaDriving = webapp de coach de corrida por IA**: usuário sobe um `.ibt` próprio + um `.ibt` de referência, o app compara telemetrias e gera insights de pilotagem em linguagem direta (ex: "na curva 10, freie 20m antes"), cruzando com uma KB proprietária mantida no NotebookLM. Stack escolhida: Python (parser) + Next.js/Vercel (webapp) + Claude API (geração de insights). Uso: Victor + 5 pessoas, não é produto público.
- **Sync de skills é opt-in por cadastro, não automático por descoberta**: qualquer projeto novo precisa (a) existir no GitHub e (b) estar listado em `config/sync-targets.yml` para receber skills automaticamente. O `co-coach-wizard` copia skills localmente na criação, mas isso é um snapshot único — sem o cadastro, futuras atualizações não chegam.
- Usuário pediu explicitamente para **não gerar arquivos de Spec-Driven Development ainda** no AuaDriving — isso foi registrado no próprio CLAUDE.md do projeto para não ser esquecido em sessões futuras.

## Arquivos relevantes
- `~/Projetos/AuaDriving/CLAUDE.md`
- `~/Projetos/AuaDriving/memory/backlog-inicial.md`
- `~/Projetos/AuaDriving/.claude/settings.json`
- `~/Projetos/AuaDriving/.claude/skills/*/SKILL.md`
- `~/Projetos/co-coach/config/sync-targets.yml`
- `~/Projetos/co-coach/.github/workflows/sync-skills.yml`
