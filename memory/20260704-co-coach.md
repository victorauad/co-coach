# Handoff — co-coach — 2026-07-04

> Este arquivo cobre duas sessões do mesmo dia. A sessão 2 (mais recente) está no final do arquivo.

## Sessão 1 — Redesign e auditoria de design

## O que foi feito nesta sessão

1. **TASKS.md — 3 itens concluídos:**
   - `docs/proficiency.json` — já existia com schema correto; corrigido bug em `build-site.py` que lia `docs/proficiency.json` (versão antiga) em vez de `static/proficiency.json` (fonte de verdade) antes de sobrescrever
   - `config/sync-targets.yml` — confirmado com os 4 repos ativos (co-coach, iracing_analysis, alamtoco, SMPL)
   - Dashboard de sync — novo step em `.github/workflows/sync-skills.yml` que gera `config/sync-status.md` (repo, skills, data) a cada push em `skills/`

2. **Auditoria `/design-is` completa** (pasta `DESIGN-IS-2026-07-02/`) das 3 interfaces (gerenciador.html, aprenda.html, index.html) — score **9/30**, veredito **REDESIGN**, com zeros em Aesthetic (#3), Honest (#6), Thorough (#8) e As-little-design (#10).

3. **Redesign executado em 6 fases** (plano em `DESIGN-IS-2026-07-02/05-plan.md`):
   - **Fase 1:** criado `static/tokens.css` — fonte única de design tokens (cor, tipo em 6 valores, espaçamento, raio, badges de tema translúcidos). `docs/style.css` agora importa via `@import url("tokens.css")` em vez de duplicar a paleta.
   - **Fase 2:** correções de honestidade — removida a afirmação falsa "em tempo real"; deletado o botão "Salvar configurações" que não salvava nada (chamava função que só mostrava um toast); corrigido `/proficiency-check` → `/co-coach-quiz` (comando real); datas do feed com rótulos distintos ("Atualizado em" vs "Último quiz em"); fallback de conteúdo para os cards sem seção "Por que importa" no markdown fonte.
   - **Fase 3:** `gerenciador.html` reescrito sem Tailwind CDN/Google Fonts — consome `tokens.css`, com `disabled`, `:focus-visible` e skeleton loading adicionados. Corrigido bug latente: `copyCmd()` chamava `showToast()` (função inexistente) em vez de `toast()`.
   - **Fase 4:** `aprenda.html` reescrito sem Tailwind/Google Fonts/Mermaid CDN (~200KB removidos) — diagrama do sistema recriado como HTML/CSS estático (grupos coloridos + setas), decisão tomada com o usuário via pergunta explícita.
   - **Fase 5:** `build-site.py` — link "⚙ Gerenciador" adicionado ao header do feed (resolvendo a ilha de navegação); CSS do feed consolidado para usar os tokens de tipo/espaçamento; `prefers-reduced-motion` respeitado.
   - **Fase 6:** verificação final — zero CDN externo, zero afirmação falsa, build limpo, validado com screenshots reais via Playwright (instalado ad-hoc em `/tmp/pw-check`, não é dependência do projeto).

4. Tudo commitado (`b86b198`) e enviado para `origin/main`.

## Estado atual

- As 3 interfaces (`docs/index.html`, `static/gerenciador.html`, `static/aprenda.html`) estão em dark mode unificado, consumindo o mesmo `tokens.css`.
- Score estimado do redesign: ~20-22/30 (estimativa informal — não foi rodado um `/design-is` de verificação pós-redesign com subagentes).
- Build (`python3 scripts/build-site.py`) roda limpo e foi validado múltiplas vezes ao longo da sessão.
- GitHub Pages deve republicar automaticamente o site com o push feito.

## Próximos passos

1. **(Opcional) Rodar `/design-is` de novo** no gerenciador/aprenda/index para confirmar o score real pós-redesign — o "~20-22/30" no resumo da sessão foi estimativa, não medição.
2. **Prioridade média pendente no TASKS.md:**
   - Skill `co-coach-mmm` para Marketing Mix Modeling
   - Ingestão de playlists do YouTube em `scripts/ingest.py`
3. **Prioridade baixa pendente:**
   - Changelog automático de KB (`kb/CHANGELOG.md`)
   - Reorganizar `skills/README.md` por categoria
   - Step de verificação nos workflows (`ingest-link.yml`, `sync-skills.yml`)
4. Verificar no navegador real (não só localhost) que o GitHub Pages publicou corretamente e que `tokens.css` está acessível em produção.

## Decisões e contexto importantes

- **Tailwind foi completamente removido**, não configurado com dark mode por cima — decisão explícita do usuário (opção "Reescrever sem Tailwind" na pergunta sobre dark mode). Motivo: resolve os 4 zeros da auditoria de uma vez e elimina dependência de CDN externo (#9).
- **Mermaid também foi removido** em favor de HTML/CSS estático — decisão explícita do usuário. Trade-off aceito: editar o diagrama agora exige editar HTML fixo em vez de sintaxe Mermaid, mas ganha-se leitura melhor em mobile (o diagrama original tinha scroll horizontal forçado) e ~200KB a menos.
- **Compromisso de escopo na Fase 3/4:** em vez de reinventar cada classe Tailwind individualmente (risco alto em ~1700 linhas de HTML), foi criado um conjunto pequeno de classes de layout estrutural (grid, flex, gap) + classes semânticas de componente (`.stat-card`, `.btn-primary`, `.kanban-card`). Isso resolve a dependência externa e a paleta, sem tentar reproduzir 100% do sistema de utilitários Tailwind manualmente.
- **Achado da auditoria original estava impreciso:** não eram "120 blocos vazios" de `Por que importa:` — eram 65/120 cards com conteúdo real e 55/120 genuinamente sem essa seção no markdown fonte (ex: docs do VS Code). Corrigido com fallback (primeiro parágrafo) em vez de só esconder o bloco.
- **`/fewer-permission-prompts` rodado no meio da sessão** (a pedido do usuário) — analisou 50 transcrições recentes; único candidato de allowlist (`gh run watch *`) já estava configurado em `.claude/settings.json`. Nada foi adicionado.
- Playwright foi instalado via `npx` num diretório temporário (`/tmp/pw-check`) só para validação visual desta sessão — não foi adicionado como dependência do projeto co-coach.

## Arquivos relevantes

- `static/tokens.css` — novo, fonte única de design tokens
- `static/gerenciador.html` — reescrito (sem Tailwind)
- `static/aprenda.html` — reescrito (sem Tailwind/Mermaid)
- `scripts/build-site.py` — CSS do feed consolidado, fallback de importancia, link de navegação, comando de quiz corrigido
- `.github/workflows/sync-skills.yml` — novo step de dashboard de sync
- `TASKS.md` — 3 itens marcados como feitos
- `DESIGN-IS-2026-07-02/` — auditoria completa (scope, evidências, scorecard, veredito, handoff, plano de 6 fases)

---

## Sessão 2 — Ingestão de stars, nova direção estratégica e gap analysis

### O que foi feito nesta sessão

1. **Indexação de GitHub Stars atualizada** — rodei `ingest-github-stars.yml`, que indexou 3 repos novos em `kb/`: `garrytan/gbrain`, `kayba-ai/agentic-context-engine`, `mermaid-js/mermaid`.
2. **Bug de CI corrigido** — `git add kb/ docs/` falhava por inteiro sempre que `docs/` estava no `.gitignore` (mesmo sendo diretório já rastreado), travando o commit inteiro nos workflows `ingest-github-stars.yml` e `ingest-link.yml`. Corrigido separando em `git add kb/` + `git add -f docs/`.
3. **Análise dos repos gbrain e ACE** — expliquei ao usuário como cada um poderia inspirar melhorias no co-coach (síntese/grafo de conhecimento; loop de aprendizado medido). Registradas como ideias no `TASKS.md` (prioridade média).
4. **Discussão de escopo do projeto** — usuário questionou se o co-coach deveria ter automações "passivas" (hooks locais). Concluímos que não: hooks locais violam a filosofia do projeto (não exigir que o usuário mexa em config/YAML). Isso levou a uma decisão maior:
5. **Nova direção estratégica formalizada na spec** — usuário está assumindo o cargo de **Head de Growth e Produto na SMPL** e quer que o co-coach:
   - Ensine Claude Code "aprendendo fazendo" (construir funcionalidades reais, sempre expostas visualmente, nunca via hooks/config que o usuário precise entender)
   - Direcione o aprendizado para a tese **Service-as-a-Software da Sequoia**
   - Atualizados: `requirements.md` (objetivos #5 e #6, novas histórias de usuário, critério de aceitação sobre hooks locais), `design.md` (regra de arquitetura "aprender fazendo" + seção de direcionamento de aprendizado + tema novo `service-as-software`), `CLAUDE.md` do projeto (seção "Direcionamento de aprendizado" + tema na lista de grep).
6. **Feature implementada: Gap Analysis da KB** — primeira feature construída sob a nova filosofia:
   - `scripts/gap-analysis.py` — conta arquivos por tema, sinaliza temas fracos (≤2 arquivos) ou parados (>60 dias sem atualização), prioriza `service-as-software` (hoje 0 arquivos)
   - Step novo em `reindex-weekly.yml` (roda toda segunda, sem hook local)
   - Aba nova "🔍 Lacunas" em `static/gerenciador.html`, seguindo o padrão visual da aba "Decisões" já existente
   - Testado localmente e via Action manual — `docs/gap-analysis.json` publicado e confirmado no ar (200 OK)
7. **Memória global atualizada** — salvei o novo cargo (Head de Growth e Produto na SMPL) e o direcionamento de aprendizado (tese Service-as-a-Software) em `~/.claude/.../memory/` para persistir entre sessões.

### Estado atual

- Tudo commitado e no `main` (últimos commits: `5999e89`, `2d9180f`, `ec7ca3f`, `1f3e130`, `5afb512`).
- Gap analysis está no ar e funcionando — `https://victorauad.github.io/co-coach/gap-analysis.json` retorna dados corretos, sinalizando `service-as-software` como lacuna de prioridade alta.
- A KB ainda não tem nenhum conteúdo com `tema: service-as-software` — é a lacuna mais óbvia detectada pelo próprio sistema que acabamos de construir.
- Arquivo `Briefing` continua não rastreado na raiz do repo (não é meu, não mexi nele — provavelmente rascunho do usuário).

### Próximos passos

1. **Preencher a lacuna que o sistema apontou**: buscar/indexar conteúdo sobre a tese Service-as-a-Software (via `/co-coach-research` ou compartilhando links) para tirar esse tema do zero.
2. Retomar o segundo item do backlog de melhorias discutido: **medir resultado das decisões no diário** (`kb/decisoes/`), inspirado no ACE — ainda não implementado, só está registrado no `TASKS.md`.
3. Considerar auditoria de conteúdo existente na KB (98 arquivos) para ver o que já se conecta à tese Service-as-a-Software, mesmo sem estar taggeado assim.
4. Validar visualmente a aba "Lacunas" no gerenciador com o usuário navegando de fato no browser (só testei via curl/leitura de código nesta sessão, não houve confirmação visual do usuário).

### Decisões e contexto importantes

- **Regra de arquitetura nova (crítica para qualquer feature futura):** nenhuma automação pode depender do usuário entender/editar hooks ou `.claude/settings.json` local. Toda automação técnica roda via GitHub Actions (servidor) e o resultado é sempre exposto por uma interface visual (feed, gerenciador, diagrama). Está documentada em `design.md`, seção "Regra de arquitetura — aprender fazendo".
- **Por que não usamos hook de SessionStart** para o gap analysis: foi cogitado, mas rejeitado porque hooks vivem só na máquina local (não sobrevivem a troca de computador) e exigem entender o conceito técnico de "hook" — contra a filosofia do projeto.
- **Novo tema `service-as-software` na KB** não é só uma tag — é o eixo central do direcionamento de aprendizado atual. Ao sugerir conteúdo, considerar esse ângulo como prioridade.
- **Lição de infraestrutura:** `git add caminho1 caminho2` falha por inteiro no Git moderno se qualquer um dos caminhos estiver no `.gitignore`, mesmo que já esteja versionado. Sempre separar em comandos `git add` distintos quando um dos caminhos for `docs/` (ou similar, gerenciado só pelo CI).

### Arquivos relevantes

- `requirements.md`, `design.md`, `CLAUDE.md` — spec atualizada com a nova direção
- `TASKS.md` — 2 itens novos de backlog (gap analysis ✅ já implementado nesta sessão, medição de decisões ainda pendente)
- `scripts/gap-analysis.py` — script novo
- `.github/workflows/reindex-weekly.yml` — step novo de gap analysis
- `.github/workflows/ingest-github-stars.yml`, `.github/workflows/ingest-link.yml` — fix do bug de `git add`
- `static/gerenciador.html` — aba "Lacunas" nova
- `~/.claude/.../memory/user_role_smpl.md`, `learning_path_service_as_software.md` — memória global sobre o novo cargo e direcionamento
