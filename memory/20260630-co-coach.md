# Handoff — co-coach — 2026-06-30

## O que foi feito nesta sessão

- **Kanban view** implementada em `static/gerenciador.html`: lê `config/sync-targets.yml` via GitHub API, busca `TASKS.md` de cada repo e renderiza 4 colunas (A Fazer / Em Progresso / Bloqueado / Feito)
- **Eliminado servidor local**: gerenciador agora funciona 100% via GitHub API + PAT no `localStorage` — sem `python scripts/server.py`
- **Modal de PAT**: tela de configuração aparece automaticamente quando não há PAT salvo
- **Helper `b64decode()`** com `TextDecoder('utf-8')` para decodificar conteúdo da API sem corromper caracteres (ã, é, ç)
- **`config/sync-targets.yml`** atualizado com 3 repos: `iracing_analysis`, `alamtoco`, `SMPL`
- **Workflow `reindex-weekly.yml`** corrigido: `git add -f docs/` + `git pull --rebase` antes do push
- **Trigger do workflow** ampliado para incluir `static/**` e `scripts/build-site.py`
- Screenshot confirmou: estrutura do Kanban renderiza corretamente (3 badges de projeto, 4 colunas, "GitHub API ativa")

## Estado atual

- Kanban **funciona estruturalmente** mas todas as colunas mostram "vazio" (count 0)
- **Causa raiz identificada**: `TASKS.md` existe localmente nos 3 repos mas **nunca foi commitado/pushado** ao GitHub → API retorna 404
  - `/Users/victorauad/Projetos/alamtoco/TASKS.md` → existe localmente, não no GitHub
  - `/Users/victorauad/Projetos/SMPL/TASKS.md` → existe localmente, não no GitHub
  - `iracing_analysis` → não tem `TASKS.md` nem localmente (a confirmar)
- **`.env` ainda não existe** no co-coach — usuário pediu criar para guardar chaves/segredos (sessão foi interrompida antes de executar)

## Próximos passos

1. Verificar se `iracing_analysis` tem `TASKS.md` local (`/Users/victorauad/Projetos/iracing_analysis/TASKS.md`)
2. Commitar e fazer push do `TASKS.md` de cada repo para o GitHub:
   - `cd /Users/victorauad/Projetos/alamtoco && git add TASKS.md && git commit -m "chore: add TASKS.md" && git push`
   - `cd /Users/victorauad/Projetos/SMPL && git add TASKS.md && git commit -m "chore: add TASKS.md" && git push`
   - Para `iracing_analysis`: criar arquivo inicial se não existir, depois push
3. Criar `.env` no co-coach para guardar chaves (ex: `GITHUB_TOKEN`, PAT)
4. Verificar no gerenciador ao vivo que as colunas do Kanban mostram as tasks

## Decisões e contexto importantes

- **PAT armazenado em `localStorage`** (chave: `gh_pat`) — não há backend; toda autenticação é client-side
- **Marcadores de status no `TASKS.md`**: `- [x]` → Feito | `- [ ] [WIP]` → Em progresso | `- [ ] [BLOQ]` → Bloqueado | `- [ ]` → A fazer | `- [-]` → ignorado
- **`docs/` está no `.gitignore`** local — nunca commitar manualmente; o CI faz `git add -f docs/` após build
- **Base64 com TextDecoder**: a abordagem antiga com `escape(atob())` corrompia UTF-8; o helper `b64decode()` é a solução correta e deve ser mantido

## Arquivos relevantes

- `static/gerenciador.html` — UI principal com Kanban, modal de PAT, skills, KB
- `config/sync-targets.yml` — lista de repos que alimentam o Kanban
- `.github/workflows/reindex-weekly.yml` — build do site e push para GitHub Pages
- `scripts/server.py` — servidor local (mantido, mas não mais necessário para o gerenciador)
- `/Users/victorauad/Projetos/alamtoco/TASKS.md` — precisa de push
- `/Users/victorauad/Projetos/SMPL/TASKS.md` — precisa de push
